# MCP Server Task List

## Status legend
todo / in-progress / done / tested / blocked

## History
Completed task entries older than the ones below are archived in task-history.md (806 lines) to keep this file cheap to read each session. Read that file only if you need the detailed rationale/notes for an old feature.

## Tasks
- [ ] Proactive: next candidate — split `lib/dispatchScan.js` (877 lines, approaching the project's file-size convention, same growth pattern that previously hit utilSchemas.js/dispatchRead.js) before it crosses further; or add a genuinely new (non-duplicate) tool — status: todo
  - notes: Not yet started. Before adding another find_*/check_*/scan_* tool, do a quick grep of existing schema descriptions to rule out near-duplicates — the scan-tool family is large enough that overlap is the default risk, not the exception.

- [x] Split lib/dispatchScan.js (877 lines) into dispatchScan.js + dispatchScan2.js — status: tested
  - notes: Boundary at find_empty_catch_blocks (mechanical split, part1=435 lines/40 requires/45 tool handlers, part2=452 lines/45 requires/40 tool handlers — verified via a scratch Node script that also cross-checked used-require detection per half; deleted after use). dispatchScan2.js exports SCAN_DISPATCH_2, merged into SCAN_DISPATCH via Object.assign in dispatchScan.js (same pattern as the prior utilSchemas.js split). Verified: key-count match against git HEAD original (85 === 85) via regex extraction, `node -c` clean on both new files, `require('./lib/executeTool')` loads cleanly, test/sections/76-pipeline-op-coverage.js regression 6/6 passing, and a scratch smoke script confirmed handlers from both halves (find_duplicates from part1, find_empty_catch_blocks/find_missing_return_after_res_send from part2) are present and callable on the merged object. README: new lib/dispatchScan2.js Code Layout row, dispatchScan.js row updated to note the merge, title v3.155.0 -> v3.156.0. package.json 4.104.0 -> 4.105.0. Committing and pushing.
- [ ] Proactive: next candidate — pick another file approaching the 500-line convention, or add a genuinely new (non-duplicate) scan/utility tool — status: todo
  - notes: Not yet started.

- [x] Proactive: next candidate — pick another file approaching the 500-line convention, or add a genuinely new (non-duplicate) scan/utility tool — status: tested (27/27 tests, all 5 rigor levels, v4.108.0)
  - notes: File-size sweep at session start found nothing over 500 lines (max was emailOps.js at 476), so picked a new tool. New lib/streamErrorHandlerOps.js: find_missing_stream_error_handler — Node fs.createReadStream/createWriteStream and http(s).request/get calls whose Readable/Writable/ClientRequest never gets a sibling `.on('error', ...)` listener attached crash the process on stream error (EventEmitter throws when 'error' is emitted with zero listeners). Checked for near-duplicates first: find_missing_websocket_error_handler covers `.on('connection', socket => ...)` handlers specifically, not generic createReadStream/createWriteStream/http.request usage — genuine gap, not overlapping. Detection: `const/let/var NAME = <call>` then brace-depth enclosing-block scan (same technique as find_missing_cleanup_on_early_return's findEnclosingBlockEnd) for `NAME.on('error'`/`NAME.once('error'`; unassigned calls checked for an inline chained `.on('error', ...)` before being skipped entirely (can't guess without a variable or visible inline handler). Calls textually inside a `pipeline(...)` wrapper are deliberately not flagged — stream.pipeline() auto-forwards every stream's errors to its own callback/rejection, documented as a caveat (textual lookback, not real call-target resolution). Wired: lib/dispatchScan2.js require+handler (after check_docker_compose_issues, default path '.'), lib/schemas/utilSchemas3.js entry, lib/schemas/execSchemas.js op-enum entry. `node -c` clean on all new/touched files, `require('./lib/executeTool')`-equivalent sanity check via dispatchScan/toolsSchema direct require confirmed the handler and schema are both present on the merged objects. New test/find-missing-stream-error-handler-tests.js — 27 tests across all 5 rigor levels (Normal — unguarded readStream/writeStream/httpRequest flagged with correct type, guarded via on('error')/once('error') not flagged, pipeline()-wrapped not flagged, inline chained .on('error') without a variable not flagged, unassigned call with no handler at all skipped; Medium — nonexistent path throws, non-number max_results throws, non-array extensions throws, empty file zero findings, extension filter excludes non-matching files, max_results truncation+truncated flag; High — unterminated call doesn't crash, top-level scope still detected, handler in a nested sibling block within the same enclosing scope still counted, single-file-path mode; Critical — shell-injection-shaped and path-traversal-shaped string arguments treated as inert text not executed/resolved, HTML/script-tag content doesn't break JSON-safe output, exact top-level key set, braces inside a string don't crash the block finder; Extreme — fuzz random bytes doesn't crash, 100 unguarded streams all detected, 50-level-deep nesting doesn't crash, 10 concurrent calls consistent). All 27/27 passing. Regression-checked test/sections/76-pipeline-op-coverage.js (6/6 passing). README: new bullet after check_docker_compose_issues, new lib/streamErrorHandlerOps.js Code Layout row, dispatchScan2.js row updated, title v3.158.0 -> v3.159.0. package.json 4.107.0 -> 4.108.0, added test:stream-error-handler script. Committing and pushing.

- [ ] Proactive: next candidate — pick another file approaching the 500-line convention, or add a genuinely new (non-duplicate) scan/utility tool — status: todo
- [x] Proactive: next candidate — pick another file approaching the 500-line convention, or add a genuinely new (non-duplicate) scan/utility tool — status: tested (25/25 tests, all 5 rigor levels, v4.109.0)
  - notes: File-size sweep found nothing over 500 lines (max dispatchScan2.js at 471, now 481 after this session — flag for next sweep). Checked lib/schemas for existing setInterval/clearInterval coverage — none found, genuine gap vs all existing find_unbounded_*/find_dangling_promises tools (none touch timers). New lib/setIntervalLeakOps.js: find_setinterval_without_clear — setInterval(...) calls whose handle is never cleared with clearInterval leak a running timer for the life of the process (classic Node long-running-server memory/zombie-timer bug). Detection: `const/let/var NAME = setInterval(...)` then a whole-file textual search for `clearInterval(NAME` (deliberately whole-file not scope-aware, since clearInterval is commonly called from a separate shutdown/cleanup function — documented false-negative tradeoff). Findings: `unassigned_interval_handle` (error, result not assigned to a variable at all so could never be cleared even in principle), `interval_never_cleared` (warning, variable exists but no matching clearInterval anywhere). An inline chained `.unref()` (assigned or not) is treated as an intentional "let this die with the process" idiom and never flagged — `.unref()`'d timers don't keep the process alive so the leak concern doesn't apply. Member-expression assignment targets (`obj.prop = setInterval(...)`) have no extractable variable name and are treated as unassigned — documented caveat. Wired: lib/dispatchScan2.js require+handler (after find_missing_stream_error_handler, default path '.'), lib/schemas/utilSchemas3.js entry, lib/schemas/execSchemas.js op-enum entry. `node -c` clean on all new/touched files, direct require of dispatchScan/toolsSchema confirmed handler+schema present on the merged objects. New test/find-setinterval-without-clear-tests.js — 25 tests across all 5 rigor levels (Normal — assigned-never-cleared warning, assigned-with-matching-clear not flagged, unassigned error, inline .unref() on both assigned/unassigned not flagged, multiple mixed intervals correct per-finding count; Medium — nonexistent path throws, non-number max_results throws, non-array extensions throws, empty file zero findings, extension filter excludes non-matching files, max_results truncation+truncated flag; High — unterminated call doesn't crash, clearInterval in a different function still suppresses (whole-file scope caveat verified), single-file-path mode, member-expression assignment treated as unassigned; Critical — shell-injection-shaped callback text never executed, HTML/script-tag content doesn't break JSON-safe output, exact top-level key set, path-traversal-shaped path argument handled, braces inside a string don't crash the scanner; Extreme — fuzz random bytes doesn't crash, 100 unassigned intervals all detected, very long single line doesn't crash, 10 concurrent calls consistent). All 25/25 passing. Regression-checked test/sections/76-pipeline-op-coverage.js (6/6 passing). README: new bullet after find_missing_stream_error_handler, new lib/setIntervalLeakOps.js Code Layout row, dispatchScan2.js row updated, title v3.159.0 -> v3.160.0. package.json 4.108.0 -> 4.109.0, added test:setinterval-leak script. Committing and pushing.

- [x] Proactive: next candidate — split lib/dispatchScan2.js (481 lines) before it crosses further, or add a genuinely new (non-duplicate) scan/utility tool — status: tested
  - notes: Split dispatchScan2.js (48 handlers, 481 lines) into dispatchScan2.js (handlers 1-24, ending at find_disabled_tls_verification) + new dispatchScan3.js (handlers 25-48, find_unpinned_docker_base_image through find_setinterval_without_clear) via a scratch Node script (deleted after use) that mechanically extracted each handler method + its used require() lines by regex, same technique as the original dispatchScan.js split. dispatchScan3.js exports SCAN_DISPATCH_3, merged into SCAN_DISPATCH_2 via Object.assign in dispatchScan2.js (same pattern dispatchScan.js already uses to merge in SCAN_DISPATCH_2) — dispatchScan.js itself needed zero changes since it only ever required SCAN_DISPATCH_2. Caught and fixed one real bug in the scratch script during this session (not shipped): the require-usage filter independently matched the shared `resolveClientPath` import into both halves, then the file-builder also unconditionally prepended its own `resolveClientPath` require line, producing a duplicate `const { resolveClientPath } = require("./roots");` at the top of both new files — caught by `node -c` (redeclaration error) before commit, fixed by deleting the extra duplicate line in each file. Verified after fix: `node -c` clean on both files, exact 48/48 handler-name match (sorted name-list comparison) against `git show HEAD:lib/dispatchScan2.js`, `require('./lib/dispatchScan')` loads cleanly with all 88 total SCAN_DISPATCH handlers present, `require('./lib/toolsSchema')` unaffected (294 schema entries, no change — this was a pure dispatch-file reorganization, no schema/require-graph changes needed since schemas were never per-dispatch-file). Regression-checked test/sections/76-pipeline-op-coverage.js (6/6 passing). Final sizes: dispatchScan2.js 256 lines, dispatchScan3.js 242 lines — both well under the 500-line convention. No new test suite needed (pure code-motion refactor, zero behavior change, already covered by each moved tool's own existing test file). README: dispatchScan2.js Code Layout row rewritten to describe the new first-half contents, new dispatchScan3.js row added, title v3.160.0 -> v3.161.0. package.json 4.109.0 -> 4.110.0. Cleaned up scratch-split.js/scratch-do-split.js/orig_dispatchScan2.js (never tracked). Committing and pushing.

- [x] Proactive: file-size sweep — status: done
  - notes: No lib/ file over 500 lines (max pdfRichExtractOps.js at 440). No split needed this session.
- [x] Proactive: add find_async_callback_in_foreach tool (async-callback-in-.forEach() footgun scan) — status: tested
  - notes: New lib/asyncForEachOps.js — two rules: foreach_inline_async_callback (error, inline async arrow/function callback passed directly to .forEach() — forEach never awaits it, internal awaits are lost to the caller) and foreach_named_async_callback (warning, bare identifier passed to .forEach() that was declared async elsewhere in the file, via the same name-collection technique find_missing_await uses). Deliberately scoped to .forEach() only (not .map/.filter — different, uncovered shape). Registered in dispatchScan3.js (SCAN_DISPATCH_3, now 89 total dispatch handlers, dispatchScan3.js still 224 lines) + schema in lib/schemas/utilSchemas3.js (TOOLS_ALL now 295 entries) + tool name added to execute_pipeline's op enum in execSchemas.js. New test/sections/128-find-async-callback-in-foreach.js, 15/15 passing across all 5 rigor levels (normal: inline arrow + inline function-expression callbacks flagged, plain sync callback not flagged; medium: max_results type-mismatch throws, non-matching-extension single-file throws; high: named async-declared callback flagged as warning with correct callback name, named non-async identifier not flagged, .map(async...) correctly out of scope, extensions filter narrows scan; critical: path traversal blocked, shell-injection-shaped text inert; extreme: max_results caps + truncated flag, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/02-jsonrpc-validation.js (13/13 passing) to confirm schema/dispatch wiring didn't break anything. node -c clean on all 4 touched/new files. README: new bullet after find_setinterval_without_clear, new lib/asyncForEachOps.js Code Layout row, dispatchScan3.js row updated, title v3.161.0 -> v3.162.0. package.json 4.110.0 -> 4.111.0. No stray .bak/temp files (gitignored). Committing and pushing.

- [x] Proactive: add find_missing_img_alt_text tool (accessibility scan — HTML/JSX <img> tags missing alt) — status: tested
  - notes: New lib/imgAltTextOps.js — first front-end-markup accessibility tool in this server (all prior 296 tools were backend/security/git/config focused). Two rules: missing_alt_attribute (error, no alt= at all) and non_descriptive_alt_text (warning, alt text is a bare filename e.g. "photo.jpg"). alt="" (decorative marker, incl. JSX alt={''}/alt={""}) correctly NOT flagged; JSX alt={variable} (dynamic) correctly NOT flagged (can't statically evaluate). Documented caveat: only literal <img tag name matched (not component wrappers like Next.js <Image>), and a stray '>' inside a JSX expression attribute value can misplace the tag boundary (single-'>' tag-end heuristic, no real parser) — explicitly tested and accepted rather than hidden. Registered in dispatchScan3.js (90 total dispatch handlers, file still 233 lines) + schema in lib/schemas/utilSchemas3.js (TOOLS_ALL now 296 entries, file 407 lines) + tool name added to execute_pipeline's op enum in execSchemas.js. New test/sections/129-find-missing-img-alt-text.js, 16/16 passing across all 5 rigor levels (normal: missing-alt flagged, descriptive alt not flagged; medium: max_results type-mismatch throws, non-matching-extension single-file throws; high: alt="" not flagged, filename-as-alt flagged as warning, JSX dynamic alt not flagged, extensions filter narrows scan; critical: path traversal blocked, injection-shaped text inert, documented stray-'>' caveat exercised without crashing; extreme: max_results caps + truncated, multiple <img> per line all detected, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/02-jsonrpc-validation.js (13/13 passing). node -c clean on all touched/new files. README: new bullet after find_async_callback_in_foreach, new lib/imgAltTextOps.js Code Layout row, dispatchScan3.js row updated, title v3.162.0 -> v3.163.0. package.json 4.111.0 -> 4.112.0. No stray .bak/temp files. Committing and pushing.

- [x] Proactive: add find_missing_form_label tool (accessibility scan — <input>/<textarea>/<select> with no accessible name) — status: tested
  - notes: New lib/formLabelOps.js — sibling to find_missing_img_alt_text, extending front-end-accessibility coverage. Single rule missing_form_label (error). Accessible name recognized via: non-empty aria-label (literal or dynamic JSX expr), aria-labelledby (trusted at face value, not resolved), matching <label for="id">/htmlFor="id" anywhere in file, or enclosing <label>...</label> span (non-greedy regex match, documented nested-label caveat). type="hidden"/"submit"/"button"/"reset" inputs skipped (never rendered or use own value as name). Covers <input>/<textarea>/<select>. Registered in dispatchScan3.js (91 total dispatch handlers, file 242 lines) + schema in lib/schemas/utilSchemas3.js (TOOLS_ALL now 297 entries, file 416 lines — approaching next split-consideration territory) + tool name added to execute_pipeline's op enum in execSchemas.js. New test/sections/130-find-missing-form-label.js, 17/17 passing across all 5 rigor levels (normal: unlabeled input flagged, aria-label'd input not flagged; medium: max_results type-mismatch throws, non-matching-extension single-file throws; high: label-for association not flagged, enclosing-label wrap not flagged, aria-labelledby not flagged, hidden/submit types skipped, textarea+select both detected, extensions filter narrows scan; critical: path traversal blocked, injection-shaped attribute inert; extreme: max_results caps + truncated, multiple controls per line all detected, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/02-jsonrpc-validation.js (13/13 passing). node -c clean on all touched/new files. README: new bullet after find_missing_img_alt_text, new lib/formLabelOps.js Code Layout row, dispatchScan3.js row updated, title v3.163.0 -> v3.164.0. package.json 4.112.0 -> 4.113.0. No stray .bak/temp files. Committing and pushing.

- [ ] Proactive: next candidate — consider splitting lib/schemas/utilSchemas3.js (416 lines, closest file to the 500-line convention after two accessibility-tool additions in a row) before it crosses further, or add another genuinely new (non-duplicate) scan/utility tool — status: todo
  - notes: Not yet started. File-size sweep as of this session: max lib/ file still pdfRichExtractOps.js (440 lines); utilSchemas3.js now 416 lines and has absorbed 3 new tool schemas across the last 2 sessions — growth rate suggests it may need a 4th schema-file split within a session or two. 297 tool names registered as of this session — grep schema descriptions first before adding new ones to rule out near-duplicates. Accessibility area now has 2 tools (img alt, form label); a natural next sibling would be missing button/link accessible-name (empty <button>/<a> with no text/aria-label) or duplicate id attribute detection across a file.

  - notes: Not yet started. File-size sweep as of this session: max lib/ file still pdfRichExtractOps.js (440 lines); lib/schemas/utilSchemas3.js now 407 lines (was ~398) — worth rechecking before it crosses 500 (likely needs a 4th-part split within a session or two at current growth rate). 296 tool names registered as of this session — grep schema descriptions first before adding new ones to rule out near-duplicates. Front-end accessibility is a promising new area now opened up (find_missing_img_alt_text) — consider siblings like missing form-label-for-input or missing button/link accessible-name checks.

- [x] Housekeeping: split oversized task.md (840 lines / 516KB) into task.md (active tail, 42 lines) + task-history.md (806 lines, archived older entries) — status: done
  - notes: task.md had grown to 516KB, making it expensive to read in full at the start of every session (violates the token-efficiency directive). Split via a PowerShell UTF-8-safe line copy (Get-Content -Encoding UTF8 / Out-File -Encoding utf8) at a clean task-boundary (line 806, right after the "run_npm_script" entry). Verified em-dash characters survived the split correctly (char code 8212, not mojibake) before committing. task-history.md holds all older completed-task notes verbatim; task.md now starts with a pointer to it. No code changes, no test impact. Future sessions: re-check task.md size periodically and re-archive if it grows past ~200 lines again.

- [x] Proactive: add find_missing_button_accessible_name tool (accessibility scan — `<button>`/`<a>` with no visible text/aria-label) — status: tested (17/17 tests, all 5 rigor levels, v4.114.0)
  - notes: Third sibling in the front-end-accessibility family (after find_missing_img_alt_text, find_missing_form_label) — this exact tool was already flagged as the natural next candidate in prior session notes, and a schema/description grep confirmed no existing tool covers button/link accessible names. New lib/buttonLinkAccessibleNameOps.js: accessible name present via visible text content (after stripping nested tags), non-empty aria-label, aria-labelledby (trusted at face value), or a nested <img>/<svg> child carrying its own alt/aria-label. `title="..."` alone is reported as a separate lower-severity `title_only_accessible_name` (warning) rather than lumped in with the hard error, since it's technically valid but a discouraged pattern. `missing_accessible_name` (error) otherwise. Wired: lib/dispatchScan3.js require+handler (after find_missing_form_label), lib/schemas/utilSchemas3.js entry, lib/schemas/execSchemas.js op-enum entry. `node -c` clean on all new/touched files; direct require of lib/dispatchScan + lib/toolsSchema confirmed handler function and schema entry both present. New test/sections/131-find-missing-button-accessible-name.js — 17/17 passing across all 5 rigor levels (Normal — icon-only button flagged, text-content button not flagged; Medium — max_results type-mismatch throws, non-matching-extension single-file throws; High — aria-label/aria-labelledby not flagged, title-only flagged as warning not error, icon-child-with-alt not flagged, <a> tag covered, extensions filter narrows scan; Critical — path traversal blocked, shell-injection-shaped attribute inert/never executed; Extreme — max_results caps+truncated flag, multiple elements per line all detected, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/76-pipeline-op-coverage.js (6/6) and test/sections/02-jsonrpc-validation.js (13/13) since schema/execSchemas files were touched — both passing. One authoring hiccup (not a code bug): the first create_file call for the new test section silently produced a 0-byte file (tool returned success but content wasn't written) — caught immediately because running it produced zero test output instead of the expected 17 "ok" lines; recreated via write_file with an explicit full-file write, verified non-zero size and correct test run before proceeding. README: new bullet after find_missing_form_label/before find_duplicate_json_keys, new lib/buttonLinkAccessibleNameOps.js Code Layout row, dispatchScan3.js row updated, title v3.164.0 -> v3.165.0 (fixed em-dash encoding while editing the title line). package.json 4.113.0 -> 4.114.0 (no separate npm test:* script — section-based tests run only via the aggregate `npm test`, consistent with all other test/sections/*.js additions). No stray .bak/temp files. Committing and pushing.

- [ ] Proactive: next candidate — pick another file approaching the 500-line convention (utilSchemas3.js now ~420+ lines), or add another genuinely new (non-duplicate) scan/utility tool — status: todo

- [x] Proactive: add find_duplicate_html_id tool (scan HTML/JSX for duplicate id="..." attribute values) — status: tested (14/14 tests, all 5 rigor levels, v4.115.0)
  - notes: File-size sweep at session start found nothing over 500 lines (max yamlOps.js at 485), so picked a new tool per prior session's suggestion. Grepped schemas first — find_duplicate_json_keys/find_duplicate_yaml_keys cover data-file key duplication only, no existing coverage for markup id duplication — genuine gap, and directly complements this server's own find_missing_form_label (whose `<label for="id">` association silently breaks on a duplicate id). New lib/duplicateIdOps.js: collects every literal id="value"/id='value' (+ JSX id={`value`}/id={"value"}/id={'value'} literal-template forms) per file, groups by value, flags any value with 2+ occurrences as `duplicate_id` (error) listing every line. JSX dynamic id={var} skipped (can't statically dedupe runtime values); checked per-file only, not cross-file. Wired: lib/dispatchScan3.js require+handler (after find_missing_button_accessible_name), lib/schemas/utilSchemas3.js entry, lib/schemas/execSchemas.js op-enum entry.
    **Hit the same create_file-writes-0-bytes bug from last session** — this time on the implementation file itself, not just the test file. `node -c` on the empty file passed trivially (empty file is valid syntax), which masked it until the actual test run threw "findDuplicateHtmlId is not a function" from inside the merged SCAN_DISPATCH object. Diagnosed via a scratch debug script (also 0 bytes on first create_file attempt, confirming the tool-level bug rather than a code bug) — recreated both lib/duplicateIdOps.js and the test section via write_file (whole-file mode), verified non-zero byte size via file_info before re-running. Lesson reinforced: after any create_file call, verify file_info shows a non-zero size before trusting `node -c` results, since syntax-checking an accidentally-empty file always "passes".
    New test/sections/132-find-duplicate-html-id.js — 14/14 passing across all 5 rigor levels (Normal — shared id flagged with correct occurrences count, unique ids not flagged; Medium — max_results type-mismatch throws, non-matching-extension single-file throws; High — 3+ duplicates report all line numbers, JSX id={`literal`} template form detected, JSX dynamic id={var} skipped, extensions filter narrows scan; Critical — path traversal blocked, shell-injection-shaped id value inert/never executed; Extreme — max_results caps+truncated flag, empty id="" not flagged, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/76-pipeline-op-coverage.js (6/6) and test/sections/02-jsonrpc-validation.js (13/13) — both passing. README: new bullet after find_missing_button_accessible_name/before find_duplicate_json_keys, new lib/duplicateIdOps.js Code Layout row, dispatchScan3.js row updated, title v3.165.0 -> v3.166.0. package.json 4.114.0 -> 4.115.0. No stray .bak/temp/_dbg files (scratch debug scripts deleted after use). Committing and pushing.

- [x] Proactive: add find_positive_tabindex tool (accessibility scan — tabindex/tabIndex > 0 anti-pattern) — status: tested (15/15 tests, all 5 rigor levels, v4.116.0)
  - notes: File-size sweep at session start: max lib/ file yamlOps.js at 486 lines, utilSchemas3.js at 436 lines - neither over the 500-line convention yet, so added a new tool per established pattern (flag yamlOps.js for next sweep). Grepped schemas for tabindex/aria-hidden/heading coverage first - none found, genuine gap; fifth-ish front-end-accessibility sibling (after find_missing_img_alt_text, find_missing_form_label, find_missing_button_accessible_name, find_duplicate_html_id). New lib/positiveTabindexOps.js: find_positive_tabindex — HTML tabindex="N"/JSX tabIndex={N} with N>0 flagged as `positive_tabindex` (warning); tabindex="0" (natural order) and tabindex="-1" (programmatic-focus-only) are the two legitimate values and never flagged; non-integer values and JSX dynamic expressions (tabIndex={var}) skipped (can't statically evaluate). Wired: lib/dispatchScan3.js require+handler (after find_duplicate_html_id), lib/schemas/utilSchemas3.js entry, lib/schemas/execSchemas.js op-enum entry. Used write_file (not create_file) for both new files and verified non-zero size via file_info before trusting node -c, per last two sessions' 0-byte-file lesson — both files wrote correctly this time. `node -c` clean on all 4 touched/new files; direct require of lib/dispatchScan + lib/toolsSchema confirmed handler function present and schema count went 299->300. New test/sections/133-find-positive-tabindex.js — 15/15 passing across all 5 rigor levels (Normal — tabindex="1" flagged with value=1, tabindex="0"/"-1" not flagged; Medium — max_results type-mismatch throws, non-matching-extension single-file throws; High — JSX tabIndex={2} detected, JSX dynamic tabIndex={var} skipped, malformed non-numeric value skipped, multiple positive-tabindex elements per file all detected, extensions filter narrows scan; Critical — path traversal blocked, shell-injection-shaped surrounding attribute text inert/never executed; Extreme — max_results caps+truncated flag, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/76-pipeline-op-coverage.js (6/6) and test/sections/02-jsonrpc-validation.js (13/13) since schema/execSchemas files were touched — both passing. README: new bullet after find_duplicate_html_id, new lib/positiveTabindexOps.js Code Layout row, dispatchScan3.js row updated, title v3.166.0 -> v3.167.0. package.json 4.115.0 -> 4.116.0. No stray .bak/temp files. Committing and pushing.

- [x] Proactive: add find_missing_rel_noopener tool (security scan — target="_blank" reverse-tabnabbing anti-pattern) — status: tested (14/14 tests, all 5 rigor levels, v4.117.0)
  - notes: File-size sweep: yamlOps.js still 486 lines (unchanged, no growth since last session), utilSchemas3.js 445 lines (now 454 after this session's addition) — neither over the 500-line convention. Inspected yamlOps.js content: it's a single cohesive recursive-descent parser (tokenizer + block/flow/scalar parsing all interdependent) — splitting it now would be an artificial cut forcing cross-file coupling of internal helpers, unlike the dispatch-file splits which cleanly separate independent tool handlers. Deferring the split until it actually crosses 500 lines. Grepped schemas for target=_blank/rel=noopener coverage first — none found, genuine gap. New lib/relNoopenerOps.js: find_missing_rel_noopener — `<a target="_blank">` with no `rel="noopener"`/`"noreferrer"` flagged as `missing_rel_noopener` (error); mirrors eslint-plugin-react's react/jsx-no-target-blank rule as a static scan. Placed in this server's security-scan family (sibling to find_open_redirect_risks/scan_cors_misconfig), NOT the accessibility family, since reverse-tabnabbing is a security concern not a screen-reader concern. JSX dynamic target={var}/rel={var} and spread {...props} are skipped (can't statically resolve; false positive worse than false negative for a review-aid scan). Wired: lib/dispatchScan3.js require+handler (after find_positive_tabindex), lib/schemas/utilSchemas3.js entry, lib/schemas/execSchemas.js op-enum entry. Hit one self-inflicted bug this session: a replace_in_file call meant to just verify a match accidentally stripped the opening `{` of the find_positive_tabindex schema object; caught immediately via size/diff check before any test ran, fixed by re-inserting the brace, verified original file size restored and node -c clean before proceeding — no bad state was ever committed. Used write_file (not create_file) for both new files, verified non-zero size via file_info. `node -c` clean on all 4 touched/new files; direct require of lib/dispatchScan + lib/toolsSchema confirmed handler present and schema count 300->301. New test/sections/134-find-missing-rel-noopener.js — 14/14 passing across all 5 rigor levels (Normal — target="_blank" with no rel flagged, rel="noopener" not flagged; Medium — max_results type-mismatch throws, non-matching-extension single-file throws; High — rel="noreferrer" also safe, JSX target={"_blank"} detected, JSX dynamic target={var} skipped, dynamic rel={var}/spread props not flagged, extensions filter narrows scan; Critical — path traversal blocked, shell-injection-shaped href text inert/never executed; Extreme — max_results caps+truncated flag, single-file mode, JSON-serialisable). Registered in test/run-tests.js require list. Regression-checked test/sections/76-pipeline-op-coverage.js (6/6) and test/sections/02-jsonrpc-validation.js (13/13) — both passing. README: new bullet after find_positive_tabindex, new lib/relNoopenerOps.js Code Layout row, dispatchScan3.js row updated, title v3.167.0 -> v3.168.0. package.json 4.116.0 -> 4.117.0. No stray .bak/temp files. Committing and pushing.

- [ ] Proactive: next candidate — pick another file approaching the 500-line convention (yamlOps.js still 486 lines, closest), or add another genuinely new (non-duplicate) scan/utility tool — status: todo
  - notes: Not yet started. yamlOps.js remains the file to watch (486 lines, cohesive parser — a real split needs care, not a mechanical cut). 301 tool names registered as of this session — grep schema descriptions first before adding new ones to rule out near-duplicates.
